{% schema %}
{
  "name": "Video Scroll",
  "settings": [
    {
      "type": "url",
      "id": "video_url",
      "label": "Video URL"
    }
  ],
  "presets": [
    {
      "name": "Video Scroll"
    }
  ]
}
{% endschema %}

{% if settings.video_url != blank %}
  <div id="set-height"></div>
  <p id="time"></p>
  <video id="v0" tabindex="0" autobuffer="autobuffer" preload="auto" poster="https://via.placeholder.com/1280x720">
    <source type="video/mp4" src="{{ settings.video_url }}">
  </video>
{% else %}
  <p>Por favor, configura un video en la sección.</p>
{% endif %}

<style>
#set-height {
  display: block;
}
#v0 {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
}
p {
  font-family: helvetica;
  font-size: 24px;
}
</style>

<script>
document.addEventListener("DOMContentLoaded", function () {
    console.log("Video URL:", "{{ settings.video_url }}");

    var playbackConst = 500, 
        setHeight = document.getElementById("set-height"), 
        vid = document.getElementById("v0"); 

    if (!vid) {
        console.log("El elemento de video no se encontró.");
        return;
    }

    console.log("Video encontrado, esperando metadata...");
    
    vid.addEventListener("loadedmetadata", function () {
        console.log("Duración del video:", vid.duration);
        setHeight.style.height = Math.floor(vid.duration) * playbackConst + "px";
    });

    function scrollPlay() {  
        if (vid) {
            var frameNumber = window.pageYOffset / playbackConst;
            vid.currentTime = frameNumber;
            console.log("Frame actual:", frameNumber);
            window.requestAnimationFrame(scrollPlay);
        }
    }

    window.requestAnimationFrame(scrollPlay);
});
</script>
